{% extends 'render/action/patterns/main-pattern.html.twig' %}

{% block line %}
    {% if action.status %}
        Vous misez {{ action.amount }}$ et <strong>vous gagnez {{ action.gain }}$</strong> à une partie de {{ action.game }} !
    {% else %}
        Vous perdez {{ action.amount }}$ à une partie de {{ action.game }}.
    {% endif %}
{% endblock %}

