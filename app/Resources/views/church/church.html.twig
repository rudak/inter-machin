{% extends 'base.html.twig' %}

{% block body %}

    <div class="container">
        <div class="row">
            <div class="col">
                <h1>Eglide de Coulonge</h1>
                <hr>
                <hr>
                <div class="row">
                    <div class="col-3">
                        <h3>Actions</h3>
                        <hr>
                        <p>
                            <a href="{{ path('church_pray') }}" class="btn btn-primary">
                                Prier
                            </a>
                        </p>
                        <p>
                            <a href="{{ path('church_steal') }}" class="btn btn-primary">
                                Voler dans la quête
                            </a>
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </div>

{% endblock %}


{% block stylesheets %}
{% endblock %}

{% block javascripts %}
    <script src="{{ asset('js/jquery.canvasjs.min.js') }}"></script>
    <script src="{{ asset('js/bank-index.js') }}"></script>
{% endblock %}