{% extends admin_layout %}

    {% block body %}
        <div class="container">
            <div class="row">
                <div class="col-sm-12">
                    <div class="page-header">
                        <h1>Voir City</h1>
                    </div>
                    <table class="table">
                        <tbody>
                        <tr>
                            <th>Id</th>
                            <td>{{ city.id }}</td>
                        </tr>
                        <tr>
                            <th>Name</th>
                            <td>{{ city.name }}</td>
                        </tr>
                        <tr>
                            <th>Price</th>
                            <td>{{ city.price }}</td>
                        </tr>
                        </tbody>
                    </table>
                    <p>
                        <a href="{{ path('admin_city_index') }}" class="btn btn-primary">
                            Retour à la liste
                        </a> <a href="{{ path('admin_city_edit', { 'id': city.id }) }}"
                                class="btn btn-success">
                            Modifier
                        </a></p>
                    <div class="delete_form_wrapper">
                        {{ form_start(delete_form) }}
                        <input type="submit" value="Supprimer" class="btn btn-danger">
                        {{ form_end(delete_form) }}
                    </div>
                </div>
            </div>
        </div>
    {% endblock %}
